---
- name: Common/base setup of env machines
  hosts: all
  tasks:
  - name: Include the example role
    vars:
      ca_install_certificates:
        - "/home/pablintino/root_ca.crt"
    ansible.builtin.include_role:
      name: ca_install

  - name: Install OS packages
    ansible.builtin.include_role:
      name: packages_install
